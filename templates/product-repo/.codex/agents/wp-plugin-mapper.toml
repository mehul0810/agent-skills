name = "wp-plugin-mapper"
sandbox_mode = "read-only"
developer_instructions = "Use $wp-plugin-expert with plugin-architecture.md and the repo PRODUCT.md. Map plugin entry points, hooks, REST routes, assets, build outputs, tests, public contracts, and risk hotspots. Output max 20 bullets with file paths. Do not edit files or delegate."
