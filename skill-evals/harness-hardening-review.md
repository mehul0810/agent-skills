# Harness Hardening Review

## Scope

Source revision `767cfe7dcfbed2fea2cbb9e0690a0a8d821a0307` fixes full scenario-contract hashing, explicit fixture binding, evaluator/scorer packet separation, conditional spatial audit routing, disputed visual ground truth, and Python runtime selection. Existing skill kernels, publication authority and proof gates remain unchanged. No automatic creativity score is introduced.

## Historical Evidence Migration

All 21 selected scenario contracts were independently read from each recorded immutable testedRevision and compared with current bytes. All matched. Original anchor hashes, tested revisions, source hashes and run-record bytes remain unchanged. Supplemental contract hashes now bind selected heading subtrees, inline cases and explicit fixture bytes. The old digests are labeled legacy-anchor-line, not represented as content hashes. Missing migration fields and body/fixture drift fail closed. The existing design-routing source change still requires a fresh run rather than being excused by migration.

## Frozen Packets

Inputs and scorer criteria are separate files. Exported packets contain only request.md, allowlisted raw artifacts and a manifest. They do not include sibling criteria files. Semantic hint detection remains a human author responsibility; filesystem separation is not an OS sandbox. The following manifest hashes were frozen before evaluator dispatch:

- Audit routing packet: `/private/tmp/audit-routing-packet-final/manifest.json`, SHA-256 `e4b148dba689ff59769bd0ba4f58f76ce4f49f7f865fabb53d34858cea74b9d3`.
- Design regression packet: `/private/tmp/design-route-packet-final/manifest.json`, SHA-256 `aacecee4eca9d7ba78a3aad2c9528914e0d1bd1a5f387656c1ca33440479f143`.

The grouping calibration no longer requires equal gaps to be called a defect. New A/B content distinguishes objectively mismatched label/value meanings from correctly associated fields with equal gaps. Prior 2/3 scores remain historical, not retroactively upgraded. This is development calibration, not held-out generalization or real-user research.

## Fresh Evaluation Results

`/root/hardened_design_regression` verified its frozen packet and skill source identity, then answered 23 cases covering all 25 existing design-routing checks. Parent scoring passed; exact run 16:48:48-16:51:56 UTC. Raw output: `/private/tmp/hardened-design-regression.txt`. The current baseline now explicitly binds its evaluator input JSON as a fixture as well as the complete scenario contracts. Old run record remains archived unchanged.

`/root/hardened_audit_visual` verified the other packet and actually loaded the spatial reference through the normal audit route. All four routing cases passed: material visual support, nonvisual exclusion, approved-inset restraint, and comparison-table preservation. In desktop/narrow rendered review it detected action dominance, unreadable instruction imagery and A's incorrect label/control meanings; preserved B and table boundaries. No false positive on the designated safe cases. Raw output `/private/tmp/hardened-audit-visual.txt` lists eight screenshots and their raster/requested viewport differences. This is development-case discrimination, not exact pixel parity or a statistical reliability claim.

Independent code reviewer `/root/harness_patch_review` found a historical Git-read buffer bug for binary fixtures above 1 MiB. It was corrected with a matching 32 MiB current/historical limit and regression tests for a 1.1 MiB valid fixture and oversized rejection. The scenario-contract suite now has 42 cases. Packet and Python focused tests also pass. Schema-level isolation does not replace author review of request meaning.

## Native Pilot: Blocked

A new task-owned default-theme site reproduced the native editor blocker independently of the earlier candidate content. Source-aware readiness agent `/root/native_pilot_readiness` used cached WordPress 6.8.8, Playground CLI 3.1.54, PHP 8.3.33 and Node 24.18.0, with an actual Editor fixture account. These versions are calibration identities, not latest-support claims. No Studio/product sites, downloads, security changes or API/store author-save shortcuts were used.

Runtime launcher enforced 127.0.0.1 and the actual listener was separately verified. Frontend heading/paragraph rendered, but the editor canvas remained gray on first open and one reload. Heading edit/save/reopen is blocked before edit. Accessibility and DOM differed on the Save disabled state, so this is not a proven Save-button failure. Only the in-app browser was enabled; root cause remains unknown across browser/Playground/WordPress. Runtime and browser tab were stopped; no listener remained.

Raw report and captures: `/private/tmp/wp-native-editor-20260916-PkXsHO/evidence/`. Editor screenshot SHA-256 `3353bf5eb7306cad768e189bbcf7c8f1fb4d542996e21bfe9298ebae337b21e8`. Launcher SHA-256 `9230ad3cea3aea7ee1c3b1519a3aedb2f8bdebe25af3f2a7cfae02618a26cb22`. Runtime lifecycle 16:45:19-16:47:25 UTC on 2026-09-16 is not measured agent cost. Temporary retention is unproven.

The full independent native implementation/repair/authoring pilot remains incomplete. Next dependency: an explicitly approved alternative browser/runtime that first passes visible native-editor calibration. Do not weaken the author gate or claim all six findings fully closed.

## Final Validation

`PYTHON=/opt/homebrew/bin/python3.13 npm test` passed, including all 42 scenario-contract regressions, packet isolation tests, Python selection/rejection tests, reference/domain checks, evidence/record validation and installation-link smoke. Shell syntax, Skill Creator site validation and Git whitespace checks passed separately. Existing low-headroom warnings remain. Full local log: `/private/tmp/harness-hardening-final-test.log`.

Source guidance was frozen before independent evaluation. The later binary-read bound repair changes the validator, not the evaluated skill bytes; its current/immutable fixture regressions pass. Twelve commits were already unpublished before this task; changes remain local pending publication scope resolution. No release, deployment, browser-security change or existing-site mutation occurred.
