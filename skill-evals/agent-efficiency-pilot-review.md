# Agent Efficiency Pilot Review

2026-09-08, native Codex CLI 0.153.4, disposable synthetic fixture under `/tmp/wp-agent-profile-pilot`; raw events retained there privately. One economical low-reasoning run requested a custom reviewer to inspect a deterministic function contract. No product mutations or release actions.

The final answer found the defect but claimed role discovery from the template's existence. Events contain three shell commands and no custom-agent dispatch. Result: task answer correct; custom-role activation and independent review UNPROVEN. Do not count this as accepted profile execution. Follow-up: use a runtime exposing named-role dispatch and verify the returned worker identity before a product pilot. No further retry was made merely to chase a successful claim.

Host-reported aggregate usage: input_tokens 113335, cached_input_tokens 77056, cache_write_input_tokens 0, output_tokens 769, reasoning_output_tokens 360. Cached input is included in input; no dollar/account-quota estimate. This single run is diagnostic, not a baseline/candidate savings comparison. The fixture directory included event output and the worker read it; future pilots must keep logs outside the allowed artifact set and minimize inherited integrations/context.

Profile validator regression: valid files pass; malformed TOML, model pins, sandbox overrides, missing role, missing subdelegation boundary and missing reasoning ceiling fail, including under `python3 -O`. Checks use explicit errors rather than removable assertions.

Prompt retirement and held-out evaluation guidance now lives in skill-evals/README.md. Preserve quality floors and exact task identity, keep optimizer access separate, and record missing telemetry explicitly. Claude-specific cache settings are not Codex configuration advice.

Fresh evaluator `/root/profiles_eval` independently read only the two updated guides and passed five scenarios: false TOML-only discovery, contaminated held-out case, lower-token quality regression, missing telemetry and incompatible provider cache controls. No actionable ambiguity found. This is guidance evaluation, not a second native pilot.
