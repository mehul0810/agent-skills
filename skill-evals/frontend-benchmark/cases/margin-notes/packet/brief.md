# Native WordPress implementation brief
Reproduce the supplied reference captures as one editable WordPress page. The references are the visual authority. Use the supplied unmodified fonts and illustration. Match each supplied viewport independently, preserving the visible hierarchy, wrapping, imagery, proportions, spacing, colors, and responsive order. Do not introduce factual claims or additional sections. Do not use a full-page screenshot as the page.

Keep the visible text, image, links, headings, and section structure editable using native WordPress blocks. Theme styling may supply presentation. Dispatch navigation and the main CTA must navigate to the dispatch section; About must navigate to the footer; Back to top returns to the page top. Preserve keyboard access and visible focus.

This packet contains no author HTML or CSS. This is a fictional editorial concept, not a real publication or endorsement. References alone do not establish implementation success or native runtime/editability proof.
