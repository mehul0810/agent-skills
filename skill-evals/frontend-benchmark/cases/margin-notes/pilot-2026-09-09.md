# First Native Execution Pilot

Tested skills/protocol revision: `9a1470df07e69ae14141fda2c7c046cc1e29c299`.
Source author: `/root/fidelity_case`; implementation: `/root/fidelity_implementation`;
independent source-blind evaluator: `/root/fidelity_evaluator`.
Source packet frozen at `2026-09-09T13:36:58.894Z`, manifest SHA-256
`593f615b60d05e00097f7aaf6963af1e40d66fa1133492d76654e27e7a7aab3d`.
Author HTML/CSS was outside the implementation/evaluation packet. Neither worker
reported reading it. The boundary was instruction-enforced, not OS isolation.

## Environment And Initial Candidate

Disposable WordPress 6.8 / PHP 8.3 using Playground CLI 3.1.53, Chrome
152.0.7977.83, DPR 1, CSS viewports 1280 x 900, 768 x 900 and 390 x 900.
These are pinned calibration versions, not current-support claims. Only synthetic
local data and local fragment CTAs were used; no existing site was modified.

The fresh implementer produced a saved Core block page and scoped theme, with no
self-directed visual repair before the first frozen candidate. Initial content
SHA-256: `b836cc9a219f39003a6a979f8c40b09d86bf467cf3bb8b8ac5f8f6ce61a007c5`.
Initial screenshot artifacts are retained in `pilot-initial/`:

| Width | Screenshot SHA-256 |
| --- | --- |
| 1280 | `c41e273cf29e8f6909b231cc275accd8cbe9b4ea6952fb7d9727fca2dbbe5c16` |
| 768 | `18aae3454372fb434dff547ebee4284fd1dc9db4582161005a3a1e3c8c89f840` |
| 390 | `4fd2905462b56878bd6cdfdc5baa96406c69d1dc383632058c379902bc2b0597` |

## Initial Independent Findings

Initial fidelity: **FAIL**, not an injected-defect fixture result.

- FD01: heading phrase wrapping differs from the source at all three widths.
- FD02: the 768px dispatch title takes two lines instead of one, with the footer
  border at y921 versus reference y884.
- FD03: the mobile index `01` splits into separate digit lines.
- Additional observed differences: logo width and CTA vertical placement.

These findings establish that initial execution was not faithful despite a native
composition and three captures. They do not establish a missing skill instruction
or justify broad wording changes without diagnosing the actual execution.

## Initial Observable Workflows

At handback `2026-09-09T13:48:36Z`, the independent evaluator reported:

| Gate | Result and scope |
| --- | --- |
| Fidelity | Fail: FD01-FD03 and additional geometry differences above |
| Visual quality | Fail: broken mobile numeric index; distinct from fidelity |
| Visitor | Blocked overall: tested tab sequence, visible 3px focus outline and local CTA passed; long edited heading at390 had no overflow; About/Back to top completion and comprehensive stress remained unproved |
| Author | Heading and link UI edits saved/reopened and appeared on frontend; media replacement and region insertion/reordering were not proved, so overall blocked |
| Editor | Readable canvas, intended font/artwork and no invalid-block recovery observed; matched-canvas-width parity not proved, so full parity blocked |
| Provenance | Blocked: exact identities and captures recorded, but final visual/spatial receipts not independently validated |

An apparent editor CTA wrapping concern (EP01) was not confirmed under matching
canvas dimensions and was not promoted to a defect. A child-image pointer
interception and disabled Apply timeout were probe limitations, not evidence of
a broken media workflow. No API/store mutation or programmatic save was counted
as author proof.

The evaluator saved a longer test heading and changed the CTA to the local footer
anchor. These task-owned test changes were handed back explicitly for restoring
the original source fixture before repair captures. Original candidate captures
remain unchanged. Full-scale side-by-side comparisons and the reopened editor
capture are retained in `pilot-review/`; the latter shows changed test content,
not source fidelity.

The evaluator's recorded interval was `13:41:17Z` through `13:48:36Z`, 439 seconds,
including freeze wait and browser/tool overhead. Runtime probing began at
approximately `13:42Z`. Host usage was unavailable, not zero. This is evaluation
duration, not an invented total implementation-time or token-efficiency metric.

## One Focused Repair

The implementation worker restored the original task-owned fixture, repaired the
reported typography/grid/flow issues and froze a rebuilt candidate. No acceptance
criteria changed. Repair1 content SHA-256:
`9f3682669698c04f2c79f93dee4aa339829e76ceecaf2cf66a752f3ac380d7d7`.
Captures are retained in `pilot-repair1/`:

| Width | Screenshot SHA-256 |
| --- | --- |
| 1280 | `138b952b62f5d409e875983794eab3cff12aebd00eddd6df629b9ba5ac0e50ad` |
| 768 | `4deb0c92db501d7e92d3e6bf6fdc0702998b812961df220afc7d649f0ce436f2` |
| 390 | `4247c5cdfeca7fa06f9c0b94fee38b44b5f8469a696e0a446a09db357ee35f5c` |

Independent read-only reproof found FD01-FD03 corrected at all required widths:
hero phrase wrapping matched, tablet dispatch/footer restored, and mobile index
stayed on one line with media starting at source y542. The evaluator still found
unresolved differences: CTA arrow approximately 9-10px left of source across
widths, mobile intro approximately 4px high, dispatch title approximately 3px low,
footer separator approximately 6px low, and total page height 1330px versus 1322px.
The offsets are visual-comparison estimates, not fabricated source DOM readings.
These were not silently accepted or normalized. **Final fidelity did not pass.**
Repair reproof interval: `13:52:04Z` through `13:52:41Z`, 37 seconds, by the same
source-blind evaluator without implementation access or candidate mutations.
Native-pixel `comparison-repair1-{1280,768,390}.png` artifacts in `pilot-review/`
preserve the final comparison, separately from initial evidence.

The pilot stopped after one independent repair cycle, not two failed cycles and
not a changed-target retry. Initial author subchecks apply only to the initial
candidate; they were not rerun against this rebuilt candidate. Media replacement,
region insertion/reordering, complete visitor flow, matched-width editor parity
and validated acceptance receipts remain unproved. No overall acceptance claim
is supported: **0 accepted / 1 attempted**, with one observed repair cycle.

## Retention And Interpretation

Source packet, initial/repaired captures and selected independent comparisons
are retained in Git. Temporary raw runtime/build artifacts remain under
`/private/tmp/wp-fidelity-candidate-sep09` and reviewer artifacts under
`/private/tmp/wp-fidelity-review-sep09`; their future availability is unproven.
Browser authentication state and credential-bearing probe scripts are excluded
from repository evidence. The disposable server is stopped after review.

This run demonstrates source-to-native execution, detection of real defects,
independent repair feedback and partial UI-only author proof. It does not prove
reliable high-fidelity delivery, complete author usability or savings. Use the
remaining clauses for a subsequent development run; use a new private source
case before claiming held-out improvement after any skill changes informed here.
