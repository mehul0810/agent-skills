# Execution Reliability Follow-Up

## Delivered Scope

- CI uses the same `npm test` aggregate as local validation, with explicit
  Python 3.11 and repository Node version. Manual dispatch, read-only permissions,
  shell syntax and commit checks remain unchanged.
- `check-global-skill-links.sh --health` aggregates link and design-policy
  failures without modifying the installation. It does not certify model loading.
- Installer alias handling now requires an observed exact macOS symlink before
  rewriting `/tmp` or `/var`. Real Linux paths retain lexical symlink checks.
- Native authoring requires observed document/node identity before mutation
  batches and coordinates a single shared-UI writer. Independently scoped APIs
  remain usable; failed identity recovery blocks only affected mutations.

## Evidence

Native source was frozen at `69d20cf22709de64057f7f28406236e4116a38c9`.
The fresh evaluator `/root/native_coordination_blind` loaded the blind packet,
owning skill and routed references, not author-side scoring criteria. All 18
decisions passed after a neutral follow-up expanded operational sequencing for
the two new cases. This was a two-turn synthetic evaluation, not native editing,
rendered quality, cross-model parity or full-task usage proof. Its parent
observation window was 99 seconds; host token telemetry was unavailable.

The native route remains 3075 words, equal to its prior size, below its unchanged
3100-word cap. This targeted consolidation does not claim that other crowded
routes have been optimized or that runtime tokens decreased.

Independent source review found the Linux alias defect; focused shell smoke,
fingerprint and health fixtures cover the repaired boundary. The live health
check detected two missing service-business skill links. Only those links were
restored, then configured installation health passed. Global policy and Pen
links were not changed.

The full local aggregate passed with Python 3.13 and Node 24, including installation
smoke, all current behavior records, evidence regressions and domain audits.
Shell syntax and diff checks passed. Hosted Linux validation is recorded separately
against the published revision; local macOS success is not Linux execution proof.

## Harness Rollout Remains Pending

Published target `3326bf098b0e327240ea1911944700c458633bc9` passes its 48 tests,
example checks and self-validation. Against this consumer it validates 117 files
and 31 scenario inventories. That structural proof is not behavior migration.

Four files in the closed validation dependency set changed: config/run-record
schemas, `src/contracts.js`, and `src/validate.js`. Existing compatibility
receipts cannot be relabeled or chained across that change. The installed pin
and historical runtime identities remain unchanged.

Under the current evidence contract, promotion needs target-bound fresh evidence
for all 23 baseline contracts and their 24 records, followed by coordinated
pin/lock/manifest validation. Most older baselines also lack standalone blind
input packets, so prepare those without exposing scoring criteria before runs.
Do not silently expand the unchanged-runtime exception to avoid this work.

Separate outstanding proof remains: exclusive native-artifact execution, a live
correction-to-outcome/recurrence loop, and actual host usage measurements. No
automation or new authority was introduced to imply those gates are complete.
